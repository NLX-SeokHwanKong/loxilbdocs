---
phase: 03-security-gateway
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - docs/security-gateway/llamafirewall.md
autonomous: true
requirements:
  - SECG-03

must_haves:
  truths:
    - "A security reviewer can find the threat model table (prompt injection, content filtering, etc.) BEFORE any configuration block on the page"
    - "The page cross-references Security Gateway pages (overview.md, presidio-pii-detection.md) in addition to AI Gateway references"
    - "The fail-open default behavior is clearly documented with a warning admonition"
    - "All 6 scanner flags are documented with their purpose and default state"
    - "mkdocs build --strict passes"
  artifacts:
    - path: "docs/security-gateway/llamafirewall.md"
      provides: "LlamaFirewall AI safety page — enhanced from existing 138-line content with Security Gateway cross-references"
      contains: "LlamaFirewallConfig"
  key_links:
    - from: "docs/security-gateway/llamafirewall.md"
      to: "docs/security-gateway/overview.md"
      via: "back-reference link"
      pattern: "overview.md"
    - from: "docs/security-gateway/llamafirewall.md"
      to: "docs/security-gateway/presidio-pii-detection.md"
      via: "complementary feature link"
      pattern: "presidio-pii-detection.md"
---

<objective>
Enhance the existing LlamaFirewall page to integrate with the Security Gateway documentation structure — adding cross-references, verifying threat model placement, and ensuring consistency with the overview page.

Purpose: The LlamaFirewall page (138 lines) was written during Phase 2 in the AI Gateway context. Phase 3 needs it to cross-reference Security Gateway pages and be consistent with the fail-mode table in overview.md. This is a lightweight enhancement, not a rewrite.

Output: Enhanced LlamaFirewall page with Security Gateway cross-references and verified structure.
</objective>

<execution_context>
@/Users/gongseoghwan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gongseoghwan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-security-gateway/03-RESEARCH.md
@.planning/phases/03-security-gateway/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance LlamaFirewall page with Security Gateway integration</name>
  <files>docs/security-gateway/llamafirewall.md</files>
  <action>
The existing llamafirewall.md (138 lines) was written during Phase 2. Perform targeted enhancements:

1. **Verify threat model is BEFORE configuration** (SECG-03 success criteria):
   - Read the existing page structure
   - The threat model table (6 scanners: prompt injection, jailbreak, code interpreter abuse, etc.) must appear before the first configuration block
   - If it's already in the correct order, leave it. If not, reorder.

2. **Verify all 6 scanner flags are documented** (from RESEARCH.md):
   - Source: pkg/llamafirewall/config.go — LlamaFirewallConfig struct
   - 6 scanner flags should be listed with their default states
   - Key defaults: server_url=localhost:50052, timeout_sec=15, fail_closed=0, block_threshold=0.9, cache_ttl=300s, connection_pool_size=10

3. **Add/update cross-references for Security Gateway context** (if missing):
   - Add "See Also" section at bottom linking to:
     - [Security Gateway Overview](overview.md) — for fail-mode comparison table
     - [Presidio PII Detection](presidio-pii-detection.md) — complementary data protection (PII vs AI safety)
     - [Configuration Reference](configuration-reference.md) — quick reference for all security features
   - If page currently only links to AI Gateway pages, add Security Gateway links alongside

4. **Verify fail-open documentation**:
   - `fail_closed: 0` means fail-OPEN (traffic passes without scan when LlamaFirewall is down)
   - This should have a `!!! warning` admonition if not already present
   - Cross-reference: "See the [fail mode comparison table](overview.md#fail-mode-reference) for how this compares to OPA and Presidio defaults."

5. **Add source annotations** if missing:
   - Key config: `LlamaFirewallConfig` from pkg/llamafirewall/config.go
   - CGO bridge: pkg/loxinet/ai_security.go

6. **Port allocation note** (if not already present):
   - LlamaFirewall defaults to port 50052
   - Presidio defaults to port 50051
   - "Ensure no port conflicts when running both services. See [port allocation table](overview.md#port-allocation)."

Do NOT rewrite the page. Only add missing elements and verify structure ordering.

Target: Existing 138 lines + 10-20 lines of enhancements = 148-158 lines.
  </action>
  <verify>
    <automated>cd /Users/gongseoghwan/go/src/loxilbdocs-fork && wc -l docs/security-gateway/llamafirewall.md | awk '{if ($1 >= 138) print "PASS"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>LlamaFirewall page has Security Gateway cross-references, threat model is before configuration, fail-open is warned, and port allocation is noted</done>
</task>

</tasks>

<verification>
1. Threat model table appears before any configuration block
2. Cross-references to overview.md and presidio-pii-detection.md exist
3. Fail-open default has a warning admonition
4. Port 50052 allocation is noted
5. Page structure is not fundamentally changed — enhancements only
</verification>

<success_criteria>
- Security reviewer finds threat model before configuration (SECG-03 success criteria met)
- Page integrates into Security Gateway section with proper cross-references
- Fail-open behavior is clearly warned
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-gateway/03-03-SUMMARY.md`
</output>
