---
phase: 07-security-gateway-api-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security-gateway/opa-policy-enforcement.md
  - docs/security-gateway/syn-flood.md
  - docs/security-gateway/ip-filtering.md
autonomous: true
requirements: [SECG-E01, SECG-E02, SECG-E03, SECG-E04, SECG-E05]

must_haves:
  truths:
    - "All 3 pages have complete REST API curl examples with request AND response JSON for every configurable feature"
    - "All 3 pages have option tables with columns: Field, Type, Valid Values, Default, Description (no Source column)"
    - "Zero Source annotations remain across all 3 pages (grep 'Source:' returns 0)"
    - "All 3 pages link to their corresponding reference/api.md section"
    - "All 3 pages follow Concept → REST API Config → Verify → Troubleshoot structure"
    - "All 3 pages have a Verify section with a curl GET command and expected response"
  artifacts:
    - path: "docs/security-gateway/opa-policy-enforcement.md"
      provides: "OPA policy enforcement with REST API config examples and Verify section"
      contains: "reference/api.md#opa-policy-watcher"
    - path: "docs/security-gateway/syn-flood.md"
      provides: "SYN flood protection with curl examples and response JSON"
      contains: "reference/api.md#security-controls"
    - path: "docs/security-gateway/ip-filtering.md"
      provides: "IP filtering with curl examples, Verify and Troubleshoot sections"
      contains: "reference/api.md#security-controls"
  key_links:
    - from: "docs/security-gateway/opa-policy-enforcement.md"
      to: "docs/reference/api.md"
      via: "markdown link"
      pattern: "reference/api\\.md#opa-policy-watcher"
    - from: "docs/security-gateway/syn-flood.md"
      to: "docs/reference/api.md"
      via: "markdown link"
      pattern: "reference/api\\.md#security-controls"
    - from: "docs/security-gateway/ip-filtering.md"
      to: "docs/reference/api.md"
      via: "markdown link"
      pattern: "reference/api\\.md#security-controls"
---

<objective>
Rewrite the 3 policy enforcement Security Gateway pages (opa-policy-enforcement.md, syn-flood.md, ip-filtering.md) to REST API-first documentation format.

Purpose: These 3 pages already have the most REST API structure among Security Gateway pages — completing them first establishes the pattern. They need: JSON blocks converted to proper curl format, response JSON added, Verify sections added, Source annotations removed, and option tables upgraded with Valid Values.

Output: Three restructured pages with complete curl examples, response JSON, Verify/Troubleshoot sections, and API reference links.
</objective>

<execution_context>
@/Users/gongseoghwan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gongseoghwan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-security-gateway-api-enhancement/07-RESEARCH.md

Source files to transform:
@docs/security-gateway/opa-policy-enforcement.md
@docs/security-gateway/syn-flood.md
@docs/security-gateway/ip-filtering.md

API reference for response examples and anchor targets:
@docs/reference/api.md (lines 335-870 for Security Gateway sections: OPA Policy Watcher ~line 335, Security Controls ~line 757)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure opa-policy-enforcement.md — add response JSON, Verify section, upgrade option table</name>
  <files>docs/security-gateway/opa-policy-enforcement.md</files>
  <action>
Transform opa-policy-enforcement.md to follow the target structure. This page is the best-structured Security Gateway page — it already has POST and GET curl examples.

1. **Remove all Source annotations** (5 locations per research). Four forms to find and remove:
   - Prose parentheticals: `(Source: *.go:*)` — delete entire parenthetical
   - Code comment annotations: `// Source: *.go:*` — remove entire comment line from code block, keep the code itself
   - Inline prose references: `Source: swagger.yml:*` or `Source: \`pkg/opa/*\`` — delete entire sentence/line
   - Table Source columns/rows: remove if present
   Post-check: `grep -n "Source:" <file>` must return 0 matches. Also grep for `pkg/opa/`, `swagger.yml` — must return 0.

2. **Add response JSON to the POST curl example.** The page already has a POST curl to `/config/opa/watcher`. Add response below:
   ```
   # Response (200): {"result": "Success"}
   ```

3. **Verify the GET curl example has complete response JSON.** Per research, the GET example already has response JSON — verify it includes `status`, `last_poll`, and `rules_count` fields. If missing, add from reference/api.md OPA Policy Watcher section (lines 375-388):
   ```
   # Response (200):
   # {
   #   "opa_url": "http://opa-server:8181",
   #   "policy_path": "loxilb/l4",
   #   "poll_interval_sec": 30,
   #   "fail_open": false,
   #   "status": "running",
   #   "last_poll": "2025-01-15T10:30:00Z"
   # }
   ```

4. **Upgrade option table.** The page has a partial table (Field, Type, Default, Description). Add a `Valid Values` column between Type and Default:
   - `opa_url`: Any HTTP/HTTPS URL
   - `policy_path`: OPA policy path string
   - `poll_interval_sec`: `> 0` (integer)
   - `fail_open`: `true`, `false`

5. **Add Verify section** after the REST API Config section:
   - Use `GET /config/opa/watcher` to confirm status is `"running"` and configuration matches.
   - Show the expected response snippet from step 3.

6. **Ensure Troubleshoot section exists.** Per research, the page already has a common issues table — keep and enhance if needed.

7. **Add API reference link in See Also:**
   - `[OPA Policy Watcher API Reference](../reference/api.md#opa-policy-watcher)`

8. **Ensure structure follows:** Concept → REST API Config → Verify → Troubleshoot → See Also
  </action>
  <verify>
    <automated>cd /Users/gongseoghwan/go/src/loxilbdocs-fork && echo "Source annotations: $(grep -c 'Source:' docs/security-gateway/opa-policy-enforcement.md)" && echo "API ref links: $(grep -c 'reference/api.md' docs/security-gateway/opa-policy-enforcement.md)" && echo "Verify section: $(grep -c '## Verify' docs/security-gateway/opa-policy-enforcement.md)" && echo "Response lines: $(grep -c 'Response' docs/security-gateway/opa-policy-enforcement.md)"</automated>
  </verify>
  <done>opa-policy-enforcement.md has zero Source annotations, POST curl has response JSON, GET curl has complete response JSON, option table has Valid Values column, has Verify section, links to reference/api.md#opa-policy-watcher</done>
</task>

<task type="auto">
  <name>Task 2: Restructure syn-flood.md and ip-filtering.md — convert JSON blocks to curl, add Verify/Troubleshoot</name>
  <files>docs/security-gateway/syn-flood.md, docs/security-gateway/ip-filtering.md</files>
  <action>
For both pages, apply these transformations:

**syn-flood.md** (3 Source annotations):

1. **Remove 3 Source annotations.** Search for all four forms (prose parentheticals, code comments, inline references, mermaid labels). Post-check: `grep -n "Source:" syn-flood.md` returns 0.

2. **Convert JSON POST blocks to proper curl format.** The page currently shows JSON blocks like:
   ```
   POST /config/securityrate
   { "synEnabled": true, ... }
   ```
   Convert to full curl:
   ```bash
   curl -X POST http://loxilb:11111/netlox/v1/config/securityrate \
     -H "Authorization: Bearer <token>" \
     -H "Content-Type: application/json" \
     -d '{
       "synEnabled": true,
       "synThreshold": 100,
       "cookieThreshold": 50
     }'

   # Response (200): {"result": "Success"}
   ```
   Use field values from reference/api.md Security Controls section (lines 793-806).

3. **Add Verify section:**
   ```bash
   curl http://loxilb:11111/netlox/v1/config/securityrate/all \
     -H "Authorization: Bearer <token>"
   ```
   Show expected response listing active security rate configs.

4. **Add Troubleshoot section** (not currently present). Include 2-3 items:
   - SYN flood protection not triggering (check synEnabled, synThreshold value)
   - Legitimate traffic blocked (check whitelistIps, adjust thresholds)

5. **Upgrade option table:** Add Valid Values column. Key values:
   - `synEnabled`: `true`, `false`
   - `synThreshold`: `> 0` (integer, packets/sec)
   - `cookieThreshold`: `> 0` (integer)
   - `connRateEnabled`: `true`, `false`
   - `whitelistIps`: Array of CIDR strings

6. **Add API reference link:** `[Security Controls API Reference](../reference/api.md#security-controls)`

7. **Ensure structure:** Concept → REST API Config → Verify → Troubleshoot → See Also

**ip-filtering.md** (2 Source annotations):

1. **Remove 2 Source annotations.** Post-check: `grep -n "Source:" ip-filtering.md` returns 0.

2. **Convert JSON POST blocks to proper curl format:**
   ```bash
   curl -X POST http://loxilb:11111/netlox/v1/config/ipfilter \
     -H "Authorization: Bearer <token>" \
     -H "Content-Type: application/json" \
     -d '{
       "filterType": "blacklist",
       "cidr": "192.0.2.0/24",
       "zone": 0,
       "priority": 100,
       "action": "drop"
     }'

   # Response (200): {"result": "Success"}
   ```

3. **Rename "Monitoring" section to "Verify".** The page already has a `GET /config/ipfilter/all` curl command — this IS the verify command. Add expected response JSON from reference/api.md (lines 330-346):
   ```
   # Response (200):
   # [
   #   {
   #     "filterType": "blacklist",
   #     "cidr": "192.0.2.0/24",
   #     "priority": 100,
   #     "packets": 0,
   #     "bytes": 0
   #   }
   # ]
   ```

4. **Add Troubleshoot section** (not currently present). Include 2-3 items:
   - Rules not matching traffic (check CIDR format, zone assignment)
   - Counters not incrementing (check rule priority order)
   - Whitelist bypass not working (check filterType is "whitelist" not "blacklist")

5. **Verify option table** has Valid Values column. Check existing "Values" column — rename to "Valid Values" if needed. Key values:
   - `filterType`: `"blacklist"`, `"whitelist"`
   - `action`: `"drop"`, `"reject"`, `"log"`
   - `zone`: `0` to N (integer)
   - `priority`: `0`-`999` (integer, lower = higher priority)

6. **Add API reference link:** `[Security Controls API Reference](../reference/api.md#security-controls)`

7. **Ensure structure:** Concept → REST API Config → Verify → Troubleshoot → See Also
  </action>
  <verify>
    <automated>cd /Users/gongseoghwan/go/src/loxilbdocs-fork && for f in docs/security-gateway/syn-flood.md docs/security-gateway/ip-filtering.md; do echo "=== $f ===" && echo "Source annotations: $(grep -c 'Source:' "$f")" && echo "API ref links: $(grep -c 'reference/api.md' "$f")" && echo "Verify section: $(grep -c '## Verify' "$f")" && echo "curl examples: $(grep -c 'curl' "$f")"; done</automated>
  </verify>
  <done>syn-flood.md has zero Source annotations, has proper curl examples with response JSON, has Verify and Troubleshoot sections, links to reference/api.md#security-controls. ip-filtering.md has zero Source annotations, has proper curl examples, Monitoring renamed to Verify with response JSON, has Troubleshoot section, links to reference/api.md#security-controls.</done>
</task>

</tasks>

<verification>
Run across all 3 files after plan completion:

```bash
# Zero Source annotations
for f in docs/security-gateway/opa-policy-enforcement.md docs/security-gateway/syn-flood.md docs/security-gateway/ip-filtering.md; do
  echo "$f: $(grep -c 'Source:' "$f") Source annotations"
done

# No source code file references remain
grep -rn "pkg/opa\|pkg/security\|swagger\.yml\|dpebpf" docs/security-gateway/opa-policy-enforcement.md docs/security-gateway/syn-flood.md docs/security-gateway/ip-filtering.md

# All have API reference links
grep -l "reference/api.md" docs/security-gateway/opa-policy-enforcement.md docs/security-gateway/syn-flood.md docs/security-gateway/ip-filtering.md | wc -l
# Expected: 3

# All have Verify sections
grep -l "## Verify" docs/security-gateway/opa-policy-enforcement.md docs/security-gateway/syn-flood.md docs/security-gateway/ip-filtering.md | wc -l
# Expected: 3

# MkDocs build
mkdocs build --strict 2>&1 | tail -5
```
</verification>

<success_criteria>
- All 3 pages have proper curl REST API examples with request JSON body and response JSON
- All 3 pages have option tables with 5 columns (Field, Type, Valid Values, Default, Description) — no Source column
- Zero Source annotations across all 3 pages (grep returns empty)
- All 3 pages link to their corresponding reference/api.md section (opa-policy-watcher or security-controls)
- All 3 pages follow Concept → REST API Config → Verify → Troubleshoot structure
- All existing conceptual content (diagrams, explanations) preserved
- mkdocs build --strict passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-gateway-api-enhancement/07-01-SUMMARY.md`
</output>
