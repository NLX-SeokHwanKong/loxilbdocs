---
phase: 07-security-gateway-api-enhancement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security-gateway/overview.md
  - docs/security-gateway/secure-dataplane.md
  - docs/security-gateway/deployment-scenarios.md
  - docs/security-gateway/configuration-reference.md
autonomous: true
requirements: [SECG-E01, SECG-E02, SECG-E03, SECG-E04, SECG-E05]

must_haves:
  truths:
    - "overview.md has zero Source annotations (including mermaid diagram labels) and follows Concept → REST API Config → Verify structure"
    - "secure-dataplane.md has zero Source annotations and cross-references individual feature Verify sections"
    - "deployment-scenarios.md has Verify sub-sections within each scenario"
    - "configuration-reference.md tables have 6 columns (Field, Type, Valid Values, Default, Description, Feature Page) with no Source column"
    - "All 4 pages link to their corresponding reference/api.md sections"
    - "Zero Source annotations remain across all 4 pages"
  artifacts:
    - path: "docs/security-gateway/overview.md"
      provides: "Security Gateway overview with REST API orientation table"
      contains: "reference/api.md"
    - path: "docs/security-gateway/secure-dataplane.md"
      provides: "Secure dataplane comparison with cross-reference Verify section"
      contains: "reference/api.md"
    - path: "docs/security-gateway/deployment-scenarios.md"
      provides: "Deployment scenarios with per-scenario Verify sub-sections"
      contains: "reference/api.md"
    - path: "docs/security-gateway/configuration-reference.md"
      provides: "Clean configuration reference tables with Valid Values column, no Source column"
      contains: "reference/api.md"
  key_links:
    - from: "docs/security-gateway/overview.md"
      to: "docs/reference/api.md"
      via: "markdown link"
      pattern: "reference/api\\.md"
    - from: "docs/security-gateway/configuration-reference.md"
      to: "docs/reference/api.md"
      via: "markdown link"
      pattern: "reference/api\\.md"
---

<objective>
Restructure the 4 structural/reference Security Gateway pages (overview.md, secure-dataplane.md, deployment-scenarios.md, configuration-reference.md) to complete the REST API-first documentation transformation.

Purpose: These pages are structural — they don't configure individual features directly. The work is: mermaid Source annotation cleanup, REST API orientation sections, cross-reference links, per-scenario Verify steps, and configuration table column changes.

Output: Four cleaned-up pages with Source annotations removed, REST API orientation, API reference links, and clean configuration tables.
</objective>

<execution_context>
@/Users/gongseoghwan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gongseoghwan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-security-gateway-api-enhancement/07-RESEARCH.md

Source files to transform:
@docs/security-gateway/overview.md
@docs/security-gateway/secure-dataplane.md
@docs/security-gateway/deployment-scenarios.md
@docs/security-gateway/configuration-reference.md

API reference for link targets:
@docs/reference/api.md (Security Gateway sections: OPA ~335, PII ~390, LlamaFirewall ~447, IPsec ~672, Security Controls ~757, SNI Certificates ~829)

Phase 6 config reference pattern (for configuration-reference.md table format):
@docs/ai-gateway/configuration-reference.md (6-column table format: Field, Type, Valid Values, Default, Description, Feature Page)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure overview.md and secure-dataplane.md — remove Source annotations, add REST API orientation</name>
  <files>docs/security-gateway/overview.md, docs/security-gateway/secure-dataplane.md</files>
  <action>
**overview.md** (3 Source annotations — all in mermaid diagram):

1. **Remove 3 mermaid dotted-line Source annotations.** Per research, these are lines like:
   ```
   B -.- B1["Source: dpebpf_linux.go"]
   ```
   Remove the entire dotted-line annotation nodes from the mermaid diagram. Keep the main flowchart nodes and connections intact.

2. **Update any prose** that references "source-traced configuration examples" — remove "source-traced" and replace with "REST API configuration examples" or similar.

3. **Add REST API Config section** after conceptual content. Since overview.md is a gateway-level overview (not a single-feature page), create an orientation table listing Security Gateway API endpoint groups:

   | Feature | API Endpoint | Reference |
   |---------|-------------|-----------|
   | OPA Policy Watcher | `POST /config/opa/watcher` | [API Reference](../reference/api.md#opa-policy-watcher) |
   | PII Detection (Presidio) | `POST /config/pii/configure` | [API Reference](../reference/api.md#pii-detection-presidio) |
   | LlamaFirewall | `POST /config/llamafirewall/enable` | [API Reference](../reference/api.md#llamafirewall) |
   | IPsec Tunnels | `POST /config/ipsec/tunnels` | [API Reference](../reference/api.md#ipsec) |
   | Security Controls | `POST /config/securityrate` | [API Reference](../reference/api.md#security-controls) |
   | IP Filtering | `POST /config/ipfilter` | [API Reference](../reference/api.md#security-controls) |
   | SNI/mTLS Certificates | `PUT /config/loadbalancer/{name}` | [API Reference](../reference/api.md#sni-certificates) |

4. **Add Verify section:** Provide a concept-level check — e.g., verify the gateway is operational by listing configured security features or checking a token validation endpoint.

5. **Add See Also with API reference links** to all Security Gateway sections in reference/api.md.

6. **Ensure structure:** Concept → REST API Config → Verify → See Also. Preserve all existing conceptual content and diagrams (minus Source annotations).

Post-check: `grep -n "Source:" overview.md` returns 0. Also grep for `dpebpf_linux` — must return 0.

**secure-dataplane.md** (2 Source annotations):

1. **Remove 2 Source annotations** (mermaid label and/or code comment). Post-check: `grep -n "Source:" secure-dataplane.md` returns 0.

2. **Add REST API Config section.** This is a conceptual comparison page — no single feature to configure. The REST API Config section should be a brief orientation:
   - "The secure dataplane combines three protection layers. Each is configured via its own REST API endpoint:"
   - IPsec: `POST /config/ipsec/tunnels` → [IPsec page](ipsec.md)
   - SYN Flood Protection: `POST /config/securityrate` → [SYN Flood page](syn-flood.md)
   - IP Filtering: `POST /config/ipfilter` → [IP Filtering page](ip-filtering.md)

3. **Add Verify section.** Cross-reference individual feature page Verify sections:
   - "To verify each layer of the secure dataplane:"
   - IPsec: See [IPsec Verify](ipsec.md#verify)
   - SYN Flood: See [SYN Flood Verify](syn-flood.md#verify)
   - IP Filtering: See [IP Filtering Verify](ip-filtering.md#verify)
   Do NOT try to create a single curl command for "the entire secure dataplane."

4. **Add API reference links:**
   - `[IPsec API Reference](../reference/api.md#ipsec)`
   - `[Security Controls API Reference](../reference/api.md#security-controls)`

5. **Ensure structure:** Concept → REST API Config → Verify → See Also. Preserve comparison content and diagrams.
  </action>
  <verify>
    <automated>cd /Users/gongseoghwan/go/src/loxilbdocs-fork && for f in docs/security-gateway/overview.md docs/security-gateway/secure-dataplane.md; do echo "=== $f ===" && echo "Source annotations: $(grep -c 'Source:' "$f")" && echo "dpebpf refs: $(grep -c 'dpebpf' "$f")" && echo "API ref links: $(grep -c 'reference/api.md' "$f")" && echo "Verify section: $(grep -c '## Verify' "$f")"; done</automated>
  </verify>
  <done>overview.md has zero Source annotations (including mermaid labels), has REST API orientation table, has Verify section, links to all Security Gateway reference/api.md sections. secure-dataplane.md has zero Source annotations, has cross-reference REST API Config and Verify sections, links to reference/api.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add Verify to deployment-scenarios.md, clean configuration-reference.md tables</name>
  <files>docs/security-gateway/deployment-scenarios.md, docs/security-gateway/configuration-reference.md</files>
  <action>
**deployment-scenarios.md** (0 Source annotations):

1. **No Source annotations to remove** (confirmed by research). Verify with grep anyway.

2. **Add Verify sub-sections** within each deployment scenario. Each scenario enables a combination of security features — add a brief Verify block at the end of each scenario showing 2-3 curl commands to confirm the scenario is functional:
   - For each scenario, identify which features are enabled (e.g., IPsec + OPA + rate limiting)
   - Show the relevant verify curl commands for those features (reuse from individual feature pages)
   - Example format within a scenario:
     ```
     ### Verify This Scenario

     Confirm IPsec tunnel is established:
     ```bash
     curl http://loxilb:11111/netlox/v1/config/ipsec/tunnels/all -H "Authorization: Bearer <token>"
     ```

     Confirm OPA watcher is running:
     ```bash
     curl http://loxilb:11111/netlox/v1/config/opa/watcher -H "Authorization: Bearer <token>"
     ```
     ```

3. **Add API reference links** relevant to each scenario's features.

4. **Ensure structure is appropriate for an operational guide.** This page does NOT need the full "Concept → REST API Config → Verify → Troubleshoot" per-feature structure. Each scenario already has configuration listed — just add the verification steps.

**configuration-reference.md** (~5 Source column entries across 9 tables):

1. **Remove Source column from all configuration tables.** The page has 9 sections (OPA Watcher, LlamaFirewall, Presidio, Rate Limiting, SecurityRateConfig, IP Filter, IPsec Global, IPsec Tunnel, mTLS Frontend, mTLS Backend). For each table:
   - Remove the `Source` column header cell
   - Remove the `Source` separator cell (`---|`)
   - Remove the `Source` data cell in every row

2. **Add Valid Values column** to each table (following Phase 6's established pattern). Final 6-column format:
   ```
   | Field | Type | Valid Values | Default | Description | Feature Page |
   ```
   Use Valid Values from the individual feature pages, reference/api.md, or sensible defaults (e.g., bool fields → `true`, `false`; string URLs → "Any URL").

3. **Standardize column headers** across all 9 tables:
   - `Field` (not Parameter)
   - `Type` (not Data Type)
   - `Valid Values` (not Range, Options)
   - `Default` (not Default Value)
   - `Description` (not Notes)
   - `Feature Page` (link to the feature documentation page)

4. **Add API reference link** in intro paragraph or See Also:
   - `[Full API Reference](../reference/api.md)` with links to relevant Security Gateway sections

5. **Verify all 9 sections are present and complete.** The sections should cover: OPA Watcher, LlamaFirewall, Presidio PII, Rate Limiting, SecurityRateConfig (SYN flood), IP Filter, IPsec Global, IPsec Tunnel, mTLS Frontend, mTLS Backend.

Post-check: `grep -c "Source:" configuration-reference.md` must be 0. Also grep for `.go:` and `common/common` — must be 0.
  </action>
  <verify>
    <automated>cd /Users/gongseoghwan/go/src/loxilbdocs-fork && for f in docs/security-gateway/deployment-scenarios.md docs/security-gateway/configuration-reference.md; do echo "=== $f ===" && echo "Source annotations: $(grep -c 'Source:' "$f")" && echo "Go file refs: $(grep -c '\.go' "$f")" && echo "API ref links: $(grep -c 'reference/api.md' "$f")"; done && echo "=== Verify sub-sections in deployment ===" && grep -c "### Verify" docs/security-gateway/deployment-scenarios.md</automated>
  </verify>
  <done>deployment-scenarios.md has Verify sub-sections within each scenario with relevant curl commands. configuration-reference.md has zero Source annotations, zero .go references, all tables have 6 columns (Field, Type, Valid Values, Default, Description, Feature Page), links to reference/api.md.</done>
</task>

</tasks>

<verification>
Run across all 4 files after plan completion:

```bash
# Zero Source annotations
for f in docs/security-gateway/overview.md docs/security-gateway/secure-dataplane.md docs/security-gateway/deployment-scenarios.md docs/security-gateway/configuration-reference.md; do
  echo "$f: $(grep -c 'Source:' "$f") Source annotations"
done

# No source code references remain
grep -rn "dpebpf\|pkg/opa\|pkg/presidio\|pkg/llamafirewall\|common/common\|swagger\.yml" docs/security-gateway/overview.md docs/security-gateway/secure-dataplane.md docs/security-gateway/deployment-scenarios.md docs/security-gateway/configuration-reference.md

# All have API reference links
grep -l "reference/api.md" docs/security-gateway/overview.md docs/security-gateway/secure-dataplane.md docs/security-gateway/deployment-scenarios.md docs/security-gateway/configuration-reference.md | wc -l
# Expected: 4

# MkDocs build
mkdocs build --strict 2>&1 | tail -5
```
</verification>

<success_criteria>
- overview.md has zero Source annotations (including mermaid labels), has REST API orientation table, links to reference/api.md sections
- secure-dataplane.md has zero Source annotations, cross-references feature pages for REST API Config and Verify
- deployment-scenarios.md has Verify sub-sections within each scenario
- configuration-reference.md tables have 6 clean columns (Field, Type, Valid Values, Default, Description, Feature Page) — no Source column
- Zero Source annotations and zero source code file references across all 4 pages
- All 4 pages link to relevant reference/api.md sections
- All existing conceptual content (diagrams, comparisons, scenario descriptions) preserved
- mkdocs build --strict passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-gateway-api-enhancement/07-03-SUMMARY.md`
</output>
